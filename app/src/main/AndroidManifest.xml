<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orojinmi.theophilus.sdch">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenuBoard"
            android:label="@string/title_activity_main_menu_board"
            android:theme="@style/AppTheme" />
        <activity android:name=".Activities.PayNow" />
        <activity android:name=".Activities.CreateAccount" />
        <activity android:name=".ScRow" />
        <activity android:name=".Activities.ScannerActivity" />
        <activity android:name=".Activities.PaymentDetail" />
        <activity android:name=".Activities.Success" />
        <activity android:name=".Activities.CheckBalance" />
        <activity android:name=".Activities.ScanQrImage" />
        <activity android:name=".Activities.Accounts" />
        <activity android:name=".Activities.History" />
        <activity android:name=".Activities.Profile" />
        <activity android:name=".Activities.Settings" />
        <activity android:name=".Activities.DetailActivity"></activity>
    </application>

</manifest>